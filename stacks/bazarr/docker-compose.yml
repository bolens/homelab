services:
  bazarr:
    image: lscr.io/linuxserver/bazarr:latest
    container_name: bazarr
    restart: unless-stopped
    cpus: 2.0
    mem_limit: 3g
    memswap_limit: 4g
    pids_limit: 512
    cpu_shares: 256
    env_file:
      - stack.env
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-America/Denver}
    volumes:
      - bazarr_config:/config
      - "${BAZARR_TV_PATH:-/mnt/unraid/media/tv}:/data/tv"
      - "${BAZARR_MOVIES_PATH:-/mnt/unraid/media/movies}:/data/movies"
    networks:
      - media-automation
      - ingress-admin
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:6767/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  media-automation:
    name: media-automation
    external: true
  ingress-admin:
    name: ingress-admin
    external: true

volumes:
  bazarr_config:
