services:
  bazarr:
    labels:
      logging: "true"
    image: lscr.io/linuxserver/bazarr:latest@sha256:b3d44d324399152be4d50775ff639a364c0fa1cfda67f71b1f38264c4d0ca09f
    container_name: bazarr
    restart: unless-stopped
    # Audio extraction for subtitle synchronization invokes ffmpeg with
    # automatic threading. Keep background work from consuming multiple cores.
    cpus: 1.0
    mem_limit: 3g
    memswap_limit: 4g
    pids_limit: 512
    cpu_shares: 256
    env_file:
      - stack.env
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-America/Denver}
    volumes:
      - bazarr_config:/config
      - "${BAZARR_TV_PATH:-/mnt/unraid/media/tv}:/data/tv"
      - "${BAZARR_MOVIES_PATH:-/mnt/unraid/media/movies}:/data/movies"
    networks:
      - media-automation
      - ingress-admin
    extra_hosts:
      # Plex uses host networking in this repository.
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:6767/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  media-automation:
    name: media-automation
    external: true
  ingress-admin:
    name: ingress-admin
    external: true

volumes:
  bazarr_config:
