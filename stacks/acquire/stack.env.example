# Acquire – optional environment variables
#
# Copy to stack.env if you want to override defaults.

# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)

# Optional: image from Harbor (default: harbor.yourdomain.com/homelab/acquire:latest)
#ACQUIRE_IMAGE=harbor.yourdomain.com/homelab/acquire:latest
