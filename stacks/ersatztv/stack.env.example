# Copy to stack.env and adjust as needed.
# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
# Existing media directory; prepare-stack.sh intentionally does not create it.
ERSATZTV_MEDIA_PATH=/path/to/media
