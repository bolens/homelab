# OnionScan – optional. Copy to .env if you want to override.
# Stack is CLI-only (no web server); run scans via: docker compose exec onionscan onionscan <addr>

# Optional: timezone (for logs)
# TZ=America/Denver
