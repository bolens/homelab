# Calibre-Web – web UI for an existing Calibre library (browse, read, download eBooks).
# Requires a Calibre library at /books (metadata.db + books). Use named volume or bind-mount.
# Access via Caddy only (no host port).

services:
  calibre-web:
    image: lscr.io/linuxserver/calibre-web:latest
    container_name: calibre-web
    restart: unless-stopped
    env_file:
      - stack.env
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-America/Denver}
    volumes:
      - calibre_web_config:/config
      - calibre_web_books:/books
    networks:
      - monitor
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8083/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 45s

networks:
  monitor:
    name: monitor
    external: true

volumes:
  calibre_web_config:
  calibre_web_books:
