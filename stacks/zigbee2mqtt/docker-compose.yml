services:
  zigbee2mqtt:
    labels:
      logging: "true"
    image: koenkk/zigbee2mqtt:latest@sha256:fef0de769dcd04c27b3a6d277b61046eb96284bdd4198dcb1687c3a01b3020f3
    container_name: zigbee2mqtt
    restart: unless-stopped
    volumes:
      - zigbee2mqtt_data:/app/data
    devices:
      # Adjust device path for your Zigbee adapter (e.g. /dev/ttyUSB0, /dev/ttyACM0)
      - /dev/ttyUSB0:/dev/ttyUSB0
    networks:
      - ingress-admin
    env_file:
      - stack.env
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  ingress-admin:
    name: ingress-admin
    external: true

volumes:
  zigbee2mqtt_data: {}
