name: spiderfoot

services:
  spiderfoot:
    image: joshuapfritz/spiderfoot:latest
    container_name: spiderfoot
    restart: unless-stopped
    cpus: 2.0
    mem_limit: 3g
    memswap_limit: 4g
    pids_limit: 512
    cpu_shares: 256
    env_file:
      - stack.env
    environment:
      - TZ=${TZ:-America/Denver}
    volumes:
      # Persistent scan data, config, API keys, etc.
      - spiderfoot-data:/data
    networks:
      - proxy-ingress
    healthcheck:
      test: ["CMD", "python3", "-c", "import socket; s = socket.create_connection(('127.0.0.1', 5001), 2); s.close()"]
      interval: 60s
      timeout: 15s
      retries: 3
      start_period: 60s

networks:
  proxy-ingress:
    name: proxy-ingress
    external: true

volumes:
  spiderfoot-data:
