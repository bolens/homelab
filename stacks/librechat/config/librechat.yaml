# Minimal LibreChat config (v1.3.4). See https://www.librechat.ai/docs/configuration/librechat_yaml
version: 1.3.4

cache: true

interface:
  mcpServers:
    placeholder: 'MCP Servers'
  privacyPolicy:
    externalUrl: 'https://librechat.ai/privacy-policy'
    openNewTab: true
  termsOfService:
    externalUrl: 'https://librechat.ai/tos'
    openNewTab: true

registration:
  socialLogins: []

endpoints:
  custom: []
