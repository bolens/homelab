# LibreChat Configuration
# Copy this file to .env and customize as needed
# Access via Caddy reverse proxy; no host ports are exposed.
# Data is stored in Docker-managed volumes (mongodb_data, redis_data, librechat_data).

# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=librechat
# Set strong passwords; do not use defaults in production
MONGO_INITDB_ROOT_PASSWORD=your-mongodb-password

# Redis Configuration
REDIS_PASSWORD=your-redis-password

# Application URLs
DOMAIN_CLIENT=http://localhost:3080
DOMAIN_SERVER=http://localhost:3080

# Ollama Configuration
# Set to http://ollama:11434 if Ollama is on the same Docker network
# Set to http://host.docker.internal:11434 if Ollama is on host
OLLAMA_BASE_URL=http://host.docker.internal:11434

# OpenAI Configuration (optional)
# If you want to use OpenAI API directly, set your API key here
# OPENAI_API_KEY=sk-your-api-key-here

# Authentication Configuration
ALLOW_REGISTRATION=true
ALLOW_SOCIAL_LOGIN=false

# Security Keys (generate with: openssl rand -base64 32)
# JWT_SECRET=
# JWT_REFRESH_SECRET=

# JWT Expiry Settings
JWT_EXPIRY=1d
JWT_REFRESH_EXPIRY=7d

# Application Title
APP_TITLE=LibreChat

# Social Login Options (set to true to enable)
VITE_SHOW_GOOGLE_LOGIN_OPTION=false
VITE_SHOW_MICROSOFT_LOGIN_OPTION=false

# Timezone
TZ=America/Denver

# Locale settings
LANG=en_US.UTF-8
LC_ALL=en_US.UTF-8
LC_CTYPE=en_US.UTF-8
