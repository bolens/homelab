# Headscale – copy to .env. Required for stack: HEADSCALE_CONFIG_B64 (base64 of config.yaml).

# -----------------------------------------------------------------------------
# Config (required for Portainer and for docker compose)
# -----------------------------------------------------------------------------
# Base64-encoded config.yaml. Generate with: base64 -w 0 config.yaml
# (or on macOS: base64 -i config.yaml | tr -d '\n')
# Create config.yaml from config.example.yaml first; set server_url and dns.base_domain.
HEADSCALE_CONFIG_B64=

# -----------------------------------------------------------------------------
# Locale & timezone (see documents/ENV-VARS.md)
# -----------------------------------------------------------------------------
TZ=America/Denver
