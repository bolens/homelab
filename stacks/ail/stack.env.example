# AIL framework – copy to stack.env if you need to override.
# The cciucd/ail-framework image runs the full stack; most config is inside the image.
# See https://github.com/ail-project/ail-framework and doc/README.md for configs.

# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
