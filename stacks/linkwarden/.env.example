# Linkwarden – copy to .env. Set NEXTAUTH_SECRET, POSTGRES_PASSWORD, MEILI_MASTER_KEY.
# See README "Generating keys and secrets" for: openssl rand -base64 32, openssl rand -base64 24, openssl rand -hex 24
# See https://docs.linkwarden.app/self-hosting/environment-variables

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------
NEXTAUTH_SECRET=      # openssl rand -base64 32
POSTGRES_PASSWORD=   # openssl rand -base64 24
MEILI_MASTER_KEY=    # openssl rand -hex 24

# Auth URL: use your public URL + /api/v1/auth (e.g. https://linkwarden.example.com/api/v1/auth)
NEXTAUTH_URL=http://localhost:3000/api/v1/auth

# -----------------------------------------------------------------------------
# Locale & timezone (see documents/ENV-VARS.md)
# -----------------------------------------------------------------------------
TZ=America/Denver
