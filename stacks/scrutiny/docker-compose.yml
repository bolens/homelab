services:
  scrutiny:
    image: ghcr.io/analogj/scrutiny:master-omnibus
    container_name: scrutiny
    restart: unless-stopped
    privileged: true  # Required for /dev access to read SMART data
    # Web UI on 8080; attach only to the dedicated Caddy ingress network.
    env_file:
      - stack.env
    environment:
      - TZ=${TZ:-America/Denver}
    volumes:
      # Configuration and collected metrics
      - scrutiny_config:/opt/scrutiny/config
      - scrutiny_influx:/opt/scrutiny/influxdb
      # Required for SMART metrics; adjust devices for your host
      - /run/udev:/run/udev:ro
    devices:
      # Example: expose all disks; you may want to restrict this list
      - /dev:/dev
    networks:
      - ingress-admin
      - mail-clients
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8080/"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 45s

networks:
  ingress-admin:
    name: ingress-admin
    external: true
  mail-clients:
    name: mail-clients
    external: true

volumes:
  scrutiny_config:
  scrutiny_influx:
