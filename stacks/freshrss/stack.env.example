# FreshRSS – copy to stack.env. Optional: set PUID/PGID if you need specific volume ownership (e.g. id your_user).

# -----------------------------------------------------------------------------
# Optional – volume ownership (LinuxServer.io); default 1000:1000
# -----------------------------------------------------------------------------
PUID=1000
PGID=1000

# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
