# Harbor is installed from the upstream bundle in stacks/harbor/harbor.
# Its active settings and secrets belong in harbor/harbor.yml, which must remain
# untracked. This file exists for repository-wide stack tooling consistency.
