# Meilisearch – fast search engine (API, typo tolerance, faceting).
# No host ports; access via Caddy (e.g. https://meilisearch.yourdomain.com → meilisearch:7700).
# Set MEILI_MASTER_KEY when exposing; optional MEILI_ENV=production.
services:
  meilisearch:
    image: getmeili/meilisearch:v1.52.0
    container_name: meilisearch
    restart: unless-stopped
    cpus: 2.0
    mem_limit: 2g
    memswap_limit: 3g
    pids_limit: 512
    cpu_shares: 256
    env_file:
      - stack.env
    environment:
      - TZ=${TZ:-America/Denver}
      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY:-}
      - MEILI_ENV=${MEILI_ENV:-development}
    volumes:
      - meilisearch_data:/meili_data
    networks:
      - ingress-public
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:7700/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  ingress-public:
    name: ingress-public
    external: true

volumes:
  meilisearch_data:
