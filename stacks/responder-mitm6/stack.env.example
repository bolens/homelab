# Responder + mitm6 – LLMNR/NBT-NS/WPAD poisoner + IPv6 MitM.
# Copy to stack.env and adjust as needed before running prepare-stack.sh.
#
# TZ/locale: provided by ../../shared.env (see documents/SHARED-RESOURCES.md)
RESPONDER_OUTPUT_PATH=${HOME}/security-lab/responder-mitm6
# Harbor fallback: RESPONDER_MITM6_IMAGE=harbor.bolens.dev/homelab/responder-mitm6:latest
