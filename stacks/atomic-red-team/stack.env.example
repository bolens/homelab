# Atomic Red Team – adversary simulation tests.
# Copy to stack.env and adjust as needed before running prepare-stack.sh.
#
# TZ/locale: provided by ../../shared.env (see documents/SHARED-RESOURCES.md)
ATOMIC_WORKSPACE_PATH=${HOME}/security-lab/atomic-red-team
# Harbor fallback: ATOMIC_RED_TEAM_IMAGE=harbor.bolens.dev/homelab/atomic-red-team:latest
