# WireGuard – remote access VPN server (LinuxServer.io).
# Exposes UDP 51820 only; no HTTP. Forward that port on your router for remote clients.
# Start: docker compose up -d

name: wireguard

services:
  wireguard:
    labels:
      logging: "true"
    image: lscr.io/linuxserver/wireguard:latest@sha256:33c5e4260f5ddf9376fcb6f1ff90c0ccc3c63d2ab469595e33d178ecf8a4c4c6
    container_name: wireguard
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    env_file:
      - stack.env
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-America/Denver}
    volumes:
      - wireguard_config:/config
      - /lib/modules:/lib/modules:ro
    ports:
      - "51820:51820/udp"
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv4.ip_forward=1
    restart: unless-stopped

volumes:
  wireguard_config: {}
