# Hashcat – GPU/CPU password recovery.
# Copy to stack.env and adjust as needed before running prepare-stack.sh.
#
# TZ/locale: provided by ../../shared.env (see documents/SHARED-RESOURCES.md)
HASHCAT_WORK_PATH=${HOME}/security-lab/hashcat/work
HASHCAT_WORDLIST_DIR=${HOME}/security-lab/wordlists
# Harbor fallback: HASHCAT_IMAGE=harbor.bolens.dev/homelab/hashcat:latest
