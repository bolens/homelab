# Mailpit – local SMTP catcher for dev/testing.
# Receives mail on port 1025; web UI on 8025. No external delivery.
# Use with Postfix: set RELAYHOST=mailpit:1025 for internal-only mailing.
# Image docs: https://mailpit.axllent.org/docs/install/docker/

services:
  mailpit:
    labels:
      logging: "true"
    image: axllent/mailpit:latest@sha256:7f33095f80e901f6ad08028f06ca284aa58fe84942be5496008d041d3b9f4d4d
    container_name: mailpit
    restart: unless-stopped
    env_file:
      - stack.env
    networks:
      - mail-clients
      - ingress-admin
    healthcheck:
      test: ["CMD", "/mailpit", "readyz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

networks:
  mail-clients:
    name: mail-clients
    external: true
  ingress-admin:
    name: ingress-admin
    external: true
