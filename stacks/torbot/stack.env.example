# OWASP TorBot – optional. Copy to stack.env if you want to override.
# No required env; Tor and TorBot run with defaults (Tor SOCKS at tor:9050).

# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
