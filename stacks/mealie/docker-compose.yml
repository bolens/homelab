# Mealie – self-hosted recipe manager and meal planner. See https://mealie.io
# Portainer-friendly: named volumes, external ingress-public network for Caddy.
# Image: https://github.com/mealie-recipes/mealie/pkgs/container/mealie

name: mealie

services:
  mealie:
    labels:
      logging: "true"
    container_name: mealie
    image: ghcr.io/mealie-recipes/mealie:latest@sha256:ba24b88462380fb59a6c7d04c6d9e607e0b6b04e31306592181186bcdab1952b
    restart: unless-stopped
    env_file:
      - stack.env
      - path: ../../shared.env
        required: false
    volumes:
      - mealie_data:/app/data
    networks:
      - ingress-public
    healthcheck:
      test: ["CMD-SHELL", "$$MEALIE_HOME/healthcheck.sh"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  ingress-public:
    name: ingress-public
    external: true

volumes:
  mealie_data:
