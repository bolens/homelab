# NetBox uses the upstream netbox-docker env files under stacks/netbox/netbox-docker.
# Keep credentials in that ignored upstream env bundle. This file exists for
# repository-wide stack tooling consistency.
