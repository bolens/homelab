# NetExec (formerly CrackMapExec) – network pentesting framework.
# Copy to stack.env and adjust as needed before running prepare-stack.sh.
#
# TZ/locale: provided by ../../shared.env (see documents/SHARED-RESOURCES.md)
NETEXEC_WORKSPACE_PATH=${HOME}/security-lab/netexec
# Harbor fallback: NETEXEC_IMAGE=harbor.bolens.dev/homelab/netexec:latest
