services:
  explo:
    image: ghcr.io/lumepart/explo:latest@sha256:5f7bc5a8266fe71a4991d79670bf35506aca5d01e6f08016c717197c09d1cf83
    container_name: explo
    restart: unless-stopped
    labels:
      - logging=true
      - logging_sampling=true
    env_file:
      - path: ../../shared.env
        required: false
      - stack.env
    volumes:
      # Explo expects a dotenv config at /opt/explo/.env.
      - ./stack.env:/opt/explo/.env:ro
      # Use the same host music library path Navidrome reads from.
      - "${MUSIC_LIBRARY_PATH:-/mnt/unraid/media/music}:/data"
      # Source path for completed slskd downloads (to migrate into /data).
      - "${SLSKD_DOWNLOADS_PATH:-/mnt/unraid/media/downloads/soulseek}:/slskd"
    networks:
      - default
      - media-services
    logging:
      driver: local
      options:
        max-size: "10m"
        max-file: "3"

networks:
  default:
    driver: bridge
  media-services:
    name: media-services
    external: true
