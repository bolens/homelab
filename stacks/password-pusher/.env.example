# Password Pusher – copy to .env and set at least PWPUSH_MASTER_KEY.
# Generate: openssl rand -base64 32  (or https://us.pwpush.com/generate_key)

# Required for encrypting sensitive data (pushes). Without it, encrypted pushes may fail.
PWPUSH_MASTER_KEY=

# Optional: timezone (default America/Denver)
# TZ=America/Denver

# Optional: when behind Caddy/reverse proxy, set your public hostname for correct links (e.g. in emails if logins are enabled)
# PWP__HOST_DOMAIN=pwpush.yourdomain.com
# PWP__HOST_PROTOCOL=https

# Optional: SMTP for confirmation emails, password resets, etc. Without this, no signup/confirmation email is sent.
# PWP__MAIL__SMTP_ADDRESS=smtp.example.com
# PWP__MAIL__SMTP_PORT=587
# PWP__MAIL__SMTP_USER_NAME=
# PWP__MAIL__SMTP_PASSWORD=
# PWP__MAIL__SMTP_AUTHENTICATION=plain
# PWP__MAIL__SMTP_STARTTLS=true
# PWP__MAIL__MAILER_SENDER="Password Pusher" <noreply@yourdomain.com>

# Optional: disable logins (anonymous pushes only). Default in image is true.
# PWP__ENABLE_LOGINS=false

# Optional: purge expired pushes after this period (e.g. "3 months", "1 year"). Default: disabled.
# PWP__PURGE_AFTER=6 months
