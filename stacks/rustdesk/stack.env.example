# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
#
# Optional server-side knobs (see RustDesk server docs / image README):
# ALWAYS_USE_RELAY=Y
#
# If hbbr runs on another host, override hbbs command in docker-compose.yml to e.g.:
#   command: ["hbbs", "-r", "relay.example.com:21117"]
