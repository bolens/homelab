# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
# Optional: LOG_LEVEL=info (debug, info, warn, error)

# Host directory for /app/config (bind mount — avoids Docker /app/config warning). Use absolute paths on Portainer.
SEERR_CONFIG_PATH=${HOME}/.config/seerr
