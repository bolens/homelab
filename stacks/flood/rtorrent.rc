# rTorrent config — imports image defaults then overrides for Docker.
# The default rc sets: socket at (basedir)/rtorrent.sock, basedir at
# /home/download/.local/share/rtorrent/ — we override port + download dir.

import = /etc/rtorrent/rtorrent.rc

# Peer port
network.port_range.set = 49160-49160
network.port_random.set = no

# Download directory
directory.default.set = /downloads

# Stay in foreground so Docker tracks the real PID.
# (system.daemon.set = true makes rTorrent fork then exit → "quickly shutting down")
