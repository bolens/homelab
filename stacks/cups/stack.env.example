# Copy to stack.env and set values. Do not commit stack.env.
#
# TZ/locale: use ../../shared.env (see documents/SHARED-RESOURCES.md)

# CUPS web admin (https://<your-caddy-hostname>:631 via Caddy uses same credentials)
CUPSADMIN=admin
CUPSPASSWORD=REPLACE_WITH_GENERATED_PASSWORD
