# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
PUID=1000
PGID=1000

# Optional: set URL base if you reverse proxy Sonarr under a path
# SONARR__InstanceName=Sonarr
# SONARR__BindAddress=*
# SONARR__Port=8989


# Unified media tree (host → /data). Libraries and downloads must share this
# mount so imports can use atomic moves/hardlinks instead of copying.
SONARR_MEDIA_PATH=/path/to/media
