name: theharvester

services:
  theharvester:
    image: secsi/restfulharvest:latest@sha256:9ffadcb9dabdb802122b3f2b8f627b256bba3dd1dbe04247959a71357f801d04
    container_name: theharvester
    restart: unless-stopped
    env_file:
      - stack.env
    environment:
      - TZ=${TZ:-America/Denver}
    networks:
      - security-research
      - ingress-admin

networks:
  security-research:
    name: security-research
    external: true
  ingress-admin:
    name: ingress-admin
    external: true
