# Absolute path is required when deploying from Portainer.
PROMTAIL_CONFIG_PATH=/home/youruser/.config/promtail/promtail-config.yml

# Stable host label used in Loki streams; do not rely on the container hostname.
HOST_HOSTNAME=your-hostname

# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)
