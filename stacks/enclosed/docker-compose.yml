services:
  enclosed:
    image: corentinth/enclosed:latest@sha256:d1e78c34ae7027c0c7b7a9ba67f352520049b9f260560fb8a0b3b1c55488cdc8
    container_name: enclosed
    restart: unless-stopped
    labels:
      - logging=true
      - logging_sampling=false
    logging:
      driver: local
      options:
        max-size: "10m"
        max-file: "3"
    env_file:
      - path: ../../shared.env
        required: false
      - stack.env
    volumes:
      - enclosed_data:/app/.data
    networks:
      - ingress-sensitive
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8787/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

    mem_limit: 512m
    cpus: "0.5"

networks:
  ingress-sensitive:
    name: ingress-sensitive
    external: true

volumes:
  enclosed_data: {}
