# TZ/locale: use shared.env (see documents/SHARED-RESOURCES.md)

# Restrict capture to the host's primary interface instead of scanning Docker
# bridges and veth devices. Replace bond0 with the default-route interface.
# NTOP_CONFIG=-i pcap:bond0 --community
