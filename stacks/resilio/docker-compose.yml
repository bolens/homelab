services:
  resilio:
    image: lscr.io/linuxserver/resilio-sync:latest@sha256:bc04a3fea189463ff9024260e984935f6f18e7b4e951ada77b0ffc362236a612
    container_name: resilio
    restart: unless-stopped
    cpus: 2.0
    mem_limit: 2g
    memswap_limit: 3g
    pids_limit: 512
    cpu_shares: 256
    env_file:
      - path: ../../shared.env
        required: false
      - stack.env
    volumes:
      - resilio_config:/config
      - type: bind
        source: ${RESILIO_DOWNLOADS_PATH:-/mnt/unraid/media/downloads/resilio}
        target: /downloads
        bind:
          create_host_path: false
      - resilio_sync:/sync
    ports:
      - "55555:55555"
    networks:
      - ingress-public
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8888/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: local
      options:
        max-size: 10m
        max-file: "3"

networks:
  ingress-public:
    name: ingress-public
    external: true

volumes:
  resilio_config:
    external: true
    name: resilio_config
  resilio_sync:
    external: true
    name: resilio_sync
