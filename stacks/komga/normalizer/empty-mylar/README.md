# Optional Mylar integration

This read-only placeholder contains no runtime configuration. Set
`MYLAR_CONFIG_PATH` to Mylar's configuration directory to enable series rechecks.
The directory must contain `config.ini` and `mylar.db` and be readable by the
normalizer's configured UID.
