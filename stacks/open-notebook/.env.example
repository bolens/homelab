# Open Notebook Configuration
# Copy this file to .env and customize as needed

# SurrealDB Configuration
SURREAL_USER=root
# Set a strong password; do not use default in production. Generate: openssl rand -base64 24
SURREAL_PASSWORD=your-surrealdb-password
SURREAL_NAMESPACE=open_notebook
SURREAL_DATABASE=open_notebook

# Open Notebook Configuration (access via Caddy; no host ports)
# Public URL the browser uses (must match Caddy host; set for open-notebook.home etc. if not using notebook.bolens.dev)
API_URL=https://notebook.yourdomain.com

# REQUIRED: Set to a secure random string. Generate: openssl rand -base64 32
OPEN_NOTEBOOK_ENCRYPTION_KEY=your-encryption-key-here

# Optional: UI login password (required after logout if set). Generate: openssl rand -base64 24
# OPEN_NOTEBOOK_PASSWORD=your-app-password

# Ollama Configuration (if using local Ollama)
# Set to http://ollama:11434 if Ollama is on the same Docker network
# Set to http://host.docker.internal:11434 if Ollama is on host
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Timezone
TZ=America/Denver

# Locale settings
LANG=en_US.UTF-8
LC_ALL=en_US.UTF-8
LC_CTYPE=en_US.UTF-8
