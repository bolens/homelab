services:
  scrypted:
    image: ghcr.io/koush/scrypted:latest@sha256:f23251dad07cf02b4b6b2e364e7948ab9fb77656e31f226186c2ba5304ebd2cd
    container_name: scrypted
    restart: unless-stopped
    network_mode: host
    cpus: 6.0
    mem_limit: 8g
    memswap_limit: 10g
    pids_limit: 2048
    cpu_shares: 512
    env_file:
      - path: ../../shared.env
        required: false
      - stack.env
    environment:
      SCRYPTED_DOCKER_AVAHI: "true"
    volumes:
      - scrypted_data:/server/volume
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "process.env.NODE_TLS_REJECT_UNAUTHORIZED='0';require('https').get('https://127.0.0.1:10443/',r=>process.exit(r.statusCode<500?0:1)).on('error',()=>process.exit(1))"
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    logging:
      driver: local
      options:
        max-size: 10m
        max-file: "3"

volumes:
  scrypted_data:
