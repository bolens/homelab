services:
  privatebin:
    labels:
      logging: "true"
    image: privatebin/nginx-fpm-alpine:latest@sha256:2dbb345178199904b80852fc68b29bb9114e6da14b20034814bb7d33520c01d8
    container_name: privatebin
    restart: unless-stopped
    cpus: 1.0
    mem_limit: 1g
    memswap_limit: 1536m
    pids_limit: 256
    cpu_shares: 256
    # No host port: access via Caddy on the dedicated ingress network.
    volumes:
      - privatebin_data:/srv/data
    networks:
      - ingress-sensitive
    env_file:
      - stack.env
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  ingress-sensitive:
    name: ingress-sensitive
    external: true

volumes:
  privatebin_data: {}
