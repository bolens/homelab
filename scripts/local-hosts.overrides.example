# Optional exceptions for scripts/sync-local-hosts.py.
# Names without a suffix get .local automatically.
#
# Include an alias even when its stack is not running or it is not a Caddy site:
# +status
#
# Exclude an alias discovered in an active stack's runtime caddy_snippet.conf:
# -admin.local
