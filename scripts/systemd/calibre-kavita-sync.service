[Unit]
Description=Export the Calibre library for Kavita
After=docker.service mnt-unraid-media.mount
Requires=docker.service
RequiresMountsFor=/mnt/unraid/media

[Service]
Type=oneshot
ExecStart=/usr/local/libexec/calibre-kavita-sync
