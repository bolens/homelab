extends: default

rules:
  document-start: disable
  line-length: disable
  # Existing Compose and generated metadata use different valid sequence styles.
  indentation: disable
  truthy:
    allowed-values: ["true", "false", "on"]
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
