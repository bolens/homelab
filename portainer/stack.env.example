# Portainer – optional. Required when using Portainer behind Caddy or another reverse proxy.
#
# Set TRUSTED_ORIGINS to one hostname only (no https://, no port). Compose adds
# the HTTPS scheme required by Portainer's absolute-origin validation.

# TRUSTED_ORIGINS=portainer.yourdomain.com
