# Portainer – optional. Required when using Portainer behind Caddy or another reverse proxy.
#
# Set TRUSTED_ORIGINS to the hostname only (no https://, no port). Comma-separate if needed.
# This fixes "Forbidden - Invalid origin" when deploying or managing stacks (Portainer 2.27.7+).

# TRUSTED_ORIGINS=portainer.yourdomain.com
